% ***Platform parameters***
p = 2;

% ***Application parameters (SDFGs)***
% number of SDF actors
nActors = 0;
% actorGraph: which application graph does the actor originate from?
actorGraph = [];
% parentActors: the parent actor of an actor in the actor's graph
parentActors = [];
% or alternatively, actorGraph and parentActors combined
parentActors2 = [];
% precedence matrix nActors x nActors
% (row i, col j)=1 means i precedes j
precedence = [| 
% SDF channels
% number of SDF channels
nChannels = 0;
% channel properties: source actor, destination actor and initial tokens
ch_sources = [];
ch_destinations = [];
initTok = [];

% Actor names
